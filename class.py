'''class student:
    def__init__(self.note)
    self.note = note 
    self.grade = []
def add_grades(self,grade):

 self.grades.append(grade)
def remove_grade(self,grade):
 if grade in self.grades:
         self.grade.remove(grade)
def average(self):
 if len(self.grades)==0:
              return 0
 return sum(self.grades) / len(self.grades)
s = student("alice")
s.add_grade(85)
s.add_grade(90)
s.add_grade(78)
print(s.average())'''

class book:
    def __init__(self, title , author):
     self.title = title
     self.author = author
    def liberary():
       liberary = liberary
       book1 = book ("1984","george")
       book2 = book ("to kill a mockinghird", "harper lee")
    add_book(book)